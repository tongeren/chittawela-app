import { CrossfadeProps } from '../../components/Crossfade/types';
import { CTAProps } from '../../components/CTA/types';

export interface HeroProps {
    sprite: string,
    crossfade: CrossfadeProps,
    cta: CTAProps
}